{
  "name": "seacows-subgraph",
  "version": "1.1.0",
  "private": "true",
  "license": "MIT",
  "scripts": {
    "build": "yarn workspaces run build",
    "release": "npx standard-version --message 'chore(release): %s\n\n[skip ci]' --no-verify"
  },
  "workspaces": [
    "packages/*"
  ]
}
